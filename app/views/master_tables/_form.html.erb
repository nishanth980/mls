<%= form_for(@master_table) do |f| %>
  <% if @master_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@master_table.errors.count, "error") %> prohibited this master_table from being saved:</h2>

      <ul>
      <% @master_table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
