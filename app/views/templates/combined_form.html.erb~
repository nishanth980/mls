	<div class="wrapper">
		<div class="temp-con">
			<div class="temp1-style">
	<%= form_tag save_form_templates_path(@pio),:method => :post, :id => "template_form" do %>
	<% c= 1 %>
	<% while c <= @x.count do %>
	
	<% @x.each do |i| %>
	<div class="test">
  <p><%= text_field_tag "temp_#{c}", "#{i}" ,:id=>"temp_#{c}" %>&nbsp;<%= select_tag "tab_#{c}",options_for_select(MasterTable.all.collect {|val| [@a=val.name, val.name]}),:prompt =>"please select a value",:id=>"tab_#{c}" %> </p>

<% c= c+1 %>
<% end %>
<% end %>
<%= submit_tag "Submit" %><br/>
<% end %>
</div>
</div>
</div>
</div>



